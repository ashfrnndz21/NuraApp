[alembic]
script_location = alembic
prepend_sys_path = .
file_template = %%(rev)s_%%(slug)s
; The URL comes from NURA_DATABASE_URL, so that one migration run can only ever reach
; one region's database. See alembic/env.py.
sqlalchemy.url =
