"""Delivery: what reaches a person, and in what order.

`feed` is the vertical feed (E21): items rendered from State, ranked with caps and quiet
hours, supplied in order — now, today, the gate, his story, learning — and paged by cursor.
`strings` is the catalogue of every line a feed card says, in his languages, each tagged
`@patient` so `make plain-words` reads it. Nothing here judges a number or a medicine: a
card repeats what the record already holds, says why it is there, and names the State it
was rendered from.
"""
