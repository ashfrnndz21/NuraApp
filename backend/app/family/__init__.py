"""Family, sharing and the helper (E12): grants by part and window, the thread and its
digests, the duty roster and tasks, the trail in his words with "only me", the push
composer, and the documents behind a basis.

Everything here is reached through the audit doors under a key context. The "only me"
floor itself lives with the keys (`app.keys.privacy`), because it is enforced in the
resolver; this package writes the rows and renders the words.
"""
