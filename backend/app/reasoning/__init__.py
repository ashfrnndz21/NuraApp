"""Reasoning: what the record, read over time, shows — as patterns to discuss, never a finding.

`ranges` is the port the trend reads reference ranges through (`ReferenceRanges`), with the
fixture table behind it until a licensed one is signed off. `trends` is the lab trend (E09-01):
a series of confirmed facts, each against the range that fits him on the day, the direction
over the last three in arithmetic, and the lines he reads — the number, the range, where it
sits when the range is the lab's own, the direction, and the doctor to ask. No model is
called here, and no line names a cause or a treatment (`.claude/rules/safety.md`).
"""
