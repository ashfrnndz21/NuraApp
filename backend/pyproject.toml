[project]
name = "nura-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn[standard]", "sqlalchemy>=2", "alembic", "pydantic>=2", "asyncpg", "httpx"]
[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "ruff", "mypy", "httpx"]
[tool.ruff]
line-length = 100
