"""Scripts the owner runs against a local server, by hand or through the Makefile.

Nothing here is imported by the app. `python -m scripts.checkpoint <n>` (or
`make checkpoint N=<n>`) walks one checkpoint from `docs/checkpoints.md` over HTTP.
"""
