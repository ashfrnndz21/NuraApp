# Progress 2026-09-13

First nightly run. The queue was created today, so there is no history behind it yet: 89 story
issues open, none closed, no branches other than `main`, no PRs open or merged.

## Merged yesterday

Nothing. `gh pr list` returns empty for both `--state open` and `--state merged`, and
`git ls-remote --heads origin` shows only `main`. The backlog issues (#1–#89) were created today
at 14:33 UTC by `scripts/backlog_to_issues.py`. The last commit on `main` is `9ecbceb`
(iOS Info.plist generation so `xcodebuild test` runs in CI), which predates the queue.

## In flight

Three, which is the cap.

| Issue | Story | Risk | Branch / PR |
|---|---|---|---|
| #1 | E00-01 Accounts with a patient-owned health graph and linked family accounts; in-region hosting | risk:high | none yet |
| #2 | E00-02 Consent capture at onboarding: explicit, revocable, versioned, exportable as a PDF | risk:high | none yet — promoted tonight |
| #3 | E00-03 Memory stores: episodic (raw artefacts), semantic (facts), working (current episode) | risk:high | none yet — promoted tonight |

**#1 needs a look.** It was labelled `agent:ready` at 14:42 UTC and again at 14:48, roughly seven
hours before this run, and has produced no branch, no PR and no comment. Either `claude-dev.yml`
did not fire on the label or the run failed before it could push. It is holding one of the three
slots without doing work. I could not confirm which from here — the Actions API returns
`HTTP 403: Resource not accessible by integration` for this token, so I cannot read run history.
Checking that workflow is the highest-value ten minutes tomorrow morning; nothing else in the
pipeline matters until a story can get from label to PR.

## What I promoted, and why

Two slots were free, so E00-02 (#2) and E00-03 (#3) went to `agent:ready`.

The selection rule is tier, then priority. Every open story is T1 and all ten dependency-free
stories are P1, so that rule alone does not separate them. I broke the tie on how much of the
backlog sits downstream of each, which also happens to agree with plain story-ID order:

- **E00-03 Memory stores** — 6 stories depend on it directly and **47 of the 89 open stories sit
  downstream of it** transitively. It is the deepest root in the graph: it gates E00-04 (the State
  model) and E02-01 (photo → OCR → extraction), and those two between them gate most of ingestion,
  medications, visits, the feed and the vertical pager. Nothing else in the queue comes close.
- **E00-02 Consent capture** — 5 direct dependents, **18 downstream**. It gates E02-05 (consult
  recording), E12-01 (roles and scoped grants) and E01-01 (onboarding), so it is the second root
  that has to exist before anything patient-facing can be built honestly.

For contrast, E00-01 (#1, already in flight) has only 3 stories downstream of it.

No story was skipped for repeated failure: there are no `agent:failed` labels anywhere in the repo,
so the twice-failed rule did not exclude anything tonight.

**One thing to be aware of:** all three in-flight stories are `risk:high`, so none of them can
auto-merge — every one will sit waiting for you. That is the correct outcome of the promotion rule
(the foundation stories are genuinely the high-risk ones) rather than a problem with it, but it
means tomorrow's queue throughput is bounded by your review time, not by the agent. If you would
rather have something moving on its own, E15-01 (#58, design tokens) is `risk:low`, dependency-free,
and has 7 stories downstream — the best leverage-per-risk in the queue. Relabelling it is your call,
not mine.

## Blocked, and why

79 stories are blocked on dependencies that are all still open, which at this stage means all of
them — nothing has merged, so nothing is unblocked. The choke points, by transitive reach:

| Story | Issue | Direct | Downstream | Status |
|---|---|---|---|---|
| E00-03 Memory stores | #3 | 6 | 47 | in flight |
| E02-01 Photo → OCR → extraction | #13 | 8 | ~30 | blocked on E00-03 |
| E00-04 State model service | #4 | 7 | ~20 | blocked on E00-03 |
| E00-02 Consent capture | #2 | 5 | 18 | in flight |
| E15-01 Design tokens | #58 | 5 | 7 | ready, not promoted |
| E00-06 Licensed drug database | #6 | 3 | 7 | ready, not promoted |

Seven dependency-free stories are ready but not promoted, purely because the three-slot cap was
full: E00-06 (#6), E00-07 (#7), E00-08 (#8), E15-01 (#58), E15-05 (#62), E19-01 (#76), E22-01 (#86).

**One story cannot ever become ready as written.** E16-03 (#66, SaMD boundary review) declares
`Depends on E09-03`, and no issue with that ID exists in this repo — the issue set jumps from
E09-01 to E10-01. So #66 is permanently blocked and no orchestrator run will pick it up. Only part
of the backlog was converted to issues (E06, E07, E08 and E20 have no issues at all, and E09 has
only E09-01), so E09-03 is most likely a T2/T3 row that was filtered out, but I could not open
`docs/feature-backlog.xlsx` from this environment to confirm whether it is a real row or a typo.
This needs a human decision: either create the E09-03 issue, or edit #66's depends-on line. Given
that #66 is the SaMD boundary review, leaving it stuck is not a neutral outcome.

## Waiting for a human

No PRs are open, so nothing is waiting on review tonight. The two items that do want a person:

1. **#1 is labelled but idle** — check `claude-dev.yml` fired, and free the slot if it did not.
2. **#66 has a dangling dependency** — decide whether E09-03 becomes an issue or comes off the line.

## Spend

Not available. The pipeline reads the day's spend from the `--output-format json` totals of the
headless runs, and no run has produced any: there are no PRs, no branches, and no spend or usage
JSON anywhere in the repo. The Actions API is also 403 for this token, so I could not fall back to
run metadata. Expect the first real spend line once a story run completes.

## Next three I would promote

In tier-then-priority order, which again resolves to story-ID order: **E00-06** (#6, licensed drug
database, 7 downstream), **E00-07** (#7, audit log service), **E00-08** (#8, offline cache). By
leverage alone E15-01 (#58) ties E00-06 and is the only `risk:low` option among them, as noted above.
